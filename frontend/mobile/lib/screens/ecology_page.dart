import 'package:flutter/material.dart';

class EcologyPage extends StatelessWidget {
  const EcologyPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Ecology Page'),
    );
  }
}
